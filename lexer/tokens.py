# token names
tokens=('SELECT','FROM','WHERE','IDENTIFIER','NUMBER','STRING','COMMA','EQ','LT','GT','LE','GE','NE','SEMICOLON')
#key words we fixed.
reserved={
        'select':'SELECT',
        'from':'FROM',
        'where':'WHERE'
}
